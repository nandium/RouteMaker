// Max photo size allowed in bytes
export const MAX_PHOTO_SIZE = 5242880;

// Max comment limit allowed per post per user
export const COMMENT_LIMIT = 1;

// Max daily number of posts per user
export const DAILY_POST_LIMIT = 7;
